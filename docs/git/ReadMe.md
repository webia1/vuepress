# Git

## First Steps

```shell
git config --global user.name We Bia
git config --global user.email "we.bia@gmail.com"
```

## `.gitignore`

```shell
folder/file.txt
generated/
*.backup
!someException.bak
```
