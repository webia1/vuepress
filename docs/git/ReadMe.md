# Git

## First Steps

```shell
git config --global user.name Max Mustermann
git config --global user.email "max.mustermann@example.com"
```

## `.gitignore`

```shell
folder/file.txt
generated/
*.backup
!someException.bak
```
